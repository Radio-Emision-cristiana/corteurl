services:
  - type: web
    name: m3u-monitor
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ACCESS_CODE
        value: "198823"
      - key: NODE_ENV
        value: production
